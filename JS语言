JS性能较低的原因：
  1. 相较于C/C++等静态语言，JS代码类型推导太难（动态类型所带来的不足，通过TS类型判断可以进行一些优化）
  2. 脚本语言的基本类型不是一个硬件上的基本类型，而是一个复合的结构和一系列的函数，用来自动转化整形、字符串、浮点数等等。
  3. 脚本语言里的一个对象也和C的结构体、C++的对象非常不一样：它通常是一个hash table，key是成员名字的字符串，value是成员或者函数。这些东西的运行时代价是非常高昂的，很多算法可以比底层语言慢两个数量级
